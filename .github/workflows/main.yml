name: primeira-pipeline

on:
  push:
    branches:
      - developer 
  workflow_dispatch: 
jobs:
  primeiro-job:
    name: "Meu primeiro job"
    runs-on: ubuntu-latest
    steps:
      - name: "Minha primeira ação"
        run: echo "hello world"
      - name: "Segunda ação"
        run: |
          echo "Você criou o seu primeiro workflow!"
          echo "Show!"
      - uses: actions/checkout@v4
      - name: "Shell Script"
        run: |
          ls -la
          chown docker:docker script.sh
          chmod 400 script.sh
          ./script.sh
        
        
          
